import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Gestion-d’imprimantes-3D',
  webDir: 'www'
};

export default config;
